<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <Button
        android:id="@+id/btnMatchEnCours"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Match en cours"
        android:layout_marginBottom="16dp"
        android:onClick="onMatchEnCoursClick"/>

    <Button
        android:id="@+id/btnMatchTermine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Match terminé"
        android:layout_below="@id/btnMatchEnCours"
        android:onClick="onMatchTermineClick"/>

</RelativeLayout>
